# Minimal deps for Computrabajo scraper (playwright, DB, config).
# Used by scrapers/Dockerfile.computrabajo to avoid installing monorepo deps.
playwright>=1.58.0
psycopg2-binary>=2.9.11
pydantic-settings>=2.12.0
sqlmodel>=0.0.32
python-dotenv>=1.2.1
